docker-compose up --build
docker-compose down


uvicorn main:app --reload 


eksctl create cluster --name projeto-nuvem-cluster-2 --region us-east-2 --nodegroup-name workers
--node-type t2.micro --nodes 2 --nodes-min 1 --nodes-max 3
